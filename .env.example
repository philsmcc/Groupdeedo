# Groupdeedo Environment Configuration

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production
BASE_URL=https://groupdeedo.com

# Database Configuration
# (SQLite is used by default, no additional config needed)

# Admin Panel Configuration
# Set a strong password for admin panel access
ADMIN_PASSWORD=YourSecureAdminPassword123!

# Auto Cleanup Configuration
# Enable automatic cleanup of old posts (optional)
ENABLE_AUTO_CLEANUP=false
CLEANUP_INTERVAL_HOURS=24
CLEANUP_DAYS_OLD=30

# Example configurations:
# Daily cleanup at 3 AM (use cron instead): ENABLE_AUTO_CLEANUP=false
# Built-in cleanup every 24 hours: ENABLE_AUTO_CLEANUP=true
# Cleanup every 6 hours: CLEANUP_INTERVAL_HOURS=6
# Delete posts older than 7 days: CLEANUP_DAYS_OLD=7